#include<iostream>
using namespace std;
int main()
{
    int num =0 ;
    cout << " enter a number";
    cin >> num;
    if (num > 0)
    cout << "The number is positive";
     if (num < 0)
    cout << "The number is negative";
    else 
    cout << "Zero";
    return 0;
}
